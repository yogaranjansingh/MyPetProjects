Simple JMH Example.
Just annotate the function to be performance tested with @Benchmark and place the class in correct folder or change the scan folder in the configuration.
To run the benchmark use the command  : "mvn clean install exec:exec . >>result.log"
